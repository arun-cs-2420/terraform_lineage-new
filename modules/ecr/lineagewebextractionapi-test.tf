/*
resource "aws_ecr_repository" "tf_ecr_repository_activity-3" {
  name = "${var.project_name}-${var.project_region}-${var.microserviceName-3}-${var.project_environment}-repo3"

  tags = {
      Name = "${var.project_name}-${var.project_region}-${var.microserviceName-3}-${var.project_environment}-repo3"
    }
  
}
*/

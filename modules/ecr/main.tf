
/*
resource "aws_ecr_repository" "tf_ecr_repository_activity" {
  name = "${var.project_name}-${var.project_region}-${var.microserviceName}-${var.project_environment}-repo"

  tags = {
      Name = "${var.project_name}-${var.project_region}-${var.microserviceName}-${var.project_environment}-repo"
    }
  
}
*/